#include <stdio.h>
#include <stdint.h>

int main (const int argc, const char** argv)
{
    uint8_t first_array[6]      = { 1, 2, 3, 4, 5, '\0' };
    char*   string              = "my string";
    char    another_string[]    = { 'a', 'b', 'c', 'd', '\0' };

    return 0;
}
